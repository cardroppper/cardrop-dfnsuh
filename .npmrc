
# Expo-compatible npm configuration
legacy-peer-deps=true

# Explicitly disable pnpm-specific configs that might be inherited
# These settings tell npm to ignore any pnpm configs from parent directories or global config
